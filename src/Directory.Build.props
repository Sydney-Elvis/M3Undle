<Project>
  <!-- Import project-specific version information so Core package releases can move independently. -->
  <Import Project="CoreVersion.props" Condition="'$(MSBuildProjectName)' == 'M3Undle.Core'" />
  <Import Project="WebVersion.props" Condition="'$(MSBuildProjectName)' == 'M3Undle.Web'" />
  
  <PropertyGroup>
    <!-- Optional: Additional metadata -->
    <Company>Sydney Elvis LLC</Company>
    <Product>M3Undle</Product>
    <Copyright>Copyright © $(Company) $([System.DateTime]::Now.Year)</Copyright>
    <Authors>M3Undle Contributors</Authors>
    
    <!-- Optional: Build information -->
    <RepositoryUrl>https://github.com/Sydney-Elvis/M3Undle</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <BuildDateUtc Condition="'$(BuildDateUtc)' == ''">$([System.DateTime]::UtcNow.ToString('yyyy-MM-ddTHH:mm:ssK'))</BuildDateUtc>
    <BuildNumber Condition="'$(BuildNumber)' == '' and '$(M3UNDLE_BUILD_NUMBER)' != ''">$(M3UNDLE_BUILD_NUMBER)</BuildNumber>
    <BuildNumber Condition="'$(BuildNumber)' == '' and '$(GITHUB_RUN_NUMBER)' != ''">$(GITHUB_RUN_NUMBER)</BuildNumber>
    <BuildNumber Condition="'$(BuildNumber)' == '' and '$(BUILD_BUILDNUMBER)' != ''">$(BUILD_BUILDNUMBER)</BuildNumber>
    <BuildNumber Condition="'$(BuildNumber)' == '' and '$(BUILD_NUMBER)' != ''">$(BUILD_NUMBER)</BuildNumber>
    <!-- Prevent git commit hash from being appended to the informational version. -->
    <IncludeSourceRevisionInInformationalVersion>false</IncludeSourceRevisionInInformationalVersion>
  </PropertyGroup>

  <ItemGroup>
    <AssemblyMetadata Include="BuildDateUtc" Value="$(BuildDateUtc)" />
    <AssemblyMetadata Include="BuildNumber" Value="$(BuildNumber)" Condition="'$(BuildNumber)' != ''" />
    <AssemblyMetadata Include="GitHash" Value="$(SourceRevisionId)" Condition="'$(SourceRevisionId)' != ''" />
  </ItemGroup>

  <!-- Embed the short git commit hash as assembly metadata at build time.
       Docker and CI can supply SourceRevisionId; local builds fall back to git. -->
  <Target Name="EmbedGitHash" BeforeTargets="GetAssemblyAttributes" Condition="'$(SourceRevisionId)' == ''">
    <Exec Command="git rev-parse --short HEAD" ConsoleToMsBuild="true" IgnoreExitCode="true" Condition="'$(_GitShortHash)' == ''">
      <Output TaskParameter="ConsoleOutput" PropertyName="_GitShortHash" />
    </Exec>
    <PropertyGroup>
      <_GitShortHash>$(_GitShortHash.Trim())</_GitShortHash>
    </PropertyGroup>
    <ItemGroup Condition="'$(_GitShortHash)' != ''">
      <AssemblyMetadata Include="GitHash" Value="$(_GitShortHash)" />
    </ItemGroup>
  </Target>

  <!-- Normalize build output paths on non-Windows to avoid literal backslash directories (for example, "bin\Debug"). -->
  <PropertyGroup Condition="$([MSBuild]::IsOSPlatform('Linux')) Or $([MSBuild]::IsOSPlatform('OSX'))">
    <BaseIntermediateOutputPath>obj/</BaseIntermediateOutputPath>
    <BaseOutputPath>bin/</BaseOutputPath>
  </PropertyGroup>
</Project>
